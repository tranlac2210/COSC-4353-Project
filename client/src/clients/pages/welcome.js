import React from 'react';
import PropTypes from 'prop-types';

welcome.propTypes = {
    
};

function welcome(props) {
    return (
        <div>
            welcome
        </div>
    );
}

export default welcome;